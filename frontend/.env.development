# Committed dev defaults for the required frontend env vars (see src/lib/env-keys.ts).
# Per-machine overrides go in .env.development.local (gitignored), which takes precedence.
VITE_API_BASE_URL=http://localhost:4000
VITE_RTC_WS_URL=ws://localhost:4001/yjs
