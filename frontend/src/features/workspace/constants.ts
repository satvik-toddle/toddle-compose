// Default title for an unnamed page; the title field shows a placeholder for it.
export const DEFAULT_PAGE_TITLE = 'Untitled';

// Doc-page readable column; PageView's title mirrors it.
export const DOC_COLUMN_WIDTH = 900;
export const DOC_SIDE_PADDING = 48;
// 40px = the prebuilt ds-doc-editor's own .ds-de-contentEditable side inset (not settable from here).
export const DOC_TEXT_INSET = DOC_SIDE_PADDING + 40;
